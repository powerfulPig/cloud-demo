spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: 100
        group: DEV
        file-extension: yaml
  application:
    name: consumer-service
server:
  port: 8002